# Web Interface Design Guidelines (100+ Rules)

## 🎨 设计规范概述

本项目采用 **Vercel Web Interface Guidelines** 官方规范，确保 UI 组件满足无障碍、性能、和用户体验的最高标准。

---

## ✅ 必须做的（影响用户体验）

### **可访问性 (Accessibility)**

- ✅ 图标按钮必须有 `aria-label`
- ✅ 表单控件要 `<label>` 或 `aria-label`
- ✅ 交互元素要支持键盘（`onKeyDown` / `onKeyUp`）
- ✅ 用 `<button>` 做操作，用 `<a>` / `<Link>` 导航（别用 `<div onClick>`）
- ✅ 图片要 `alt` 属性（装饰图用 `alt=""` 或 `aria-hidden="true"`）
- ✅ 异步更新需要 `aria-live="polite"`（Toast、表单验证）
- ✅ 优先用语义 HTML（`<button>`, `<label>`, `<table>`）而不是 ARIA
- ✅ 标题要分级（`<h1>` → `<h6>`），主要内容前加 Skip Link

---

### **焦点状态 (Focus States)**

- ✅ `focus-visible:ring-*` 给所有交互元素
- ❌ **绝不** `outline-none` 而不加替代品
- ✅ 用 `:focus-visible` 而不是 `:focus`（避免点击时出现焦点框）
- ✅ 组合控件用 `:focus-within`

---

### **表单 (Forms)**

- ✅ 输入框要 `autocomplete` 和 `name`
- ✅ 用对应的 `type`（`email`, `tel`, `url`, `number`） 和 `inputmode`
- ❌ 别禁用粘贴（`onPaste` + `preventDefault`）
- ✅ label 可点击（`htmlFor` 或包裹控件）
- ✅ 邮箱/验证码/用户名禁用拼写检查（`spellCheck={false}`）
- ✅ 提交按钮在请求开始前保持启用，请求中显示 spinner
- ✅ 错误信息内联显示在字段旁，回车后焦点到第一个错误
- ✅ Placeholder 以 `…` 结尾，展示格式示例

---

### **动画 (Animation)**

- ✅ 尊重 `prefers-reduced-motion`（提供简化版或禁用）
- ✅ **仅** 改 `transform` / `opacity`（GPU 友好）
- ❌ **别用** `transition: all`，列出具体属性
- ✅ SVG 动画：在 `<g>` wrapper 上加 `transform`，设置 `transform-box: fill-box; transform-origin: center`
- ✅ 动画可被中断——用户输入时立即响应

---

### **排版 (Typography)**

- ✅ `…` 而不是 `...`（省略号）
- ✅ 智能引号 `"` `"` 而不是直引号 `"`
- ✅ 非断行空格：`10&nbsp;MB`, `⌘&nbsp;K`, 品牌名称中使用
- ✅ Loading 文字以 `…` 结尾：`"Loading…"`, `"Saving…"`
- ✅ 数字列：用 `font-variant-numeric: tabular-nums`（对齐）
- ✅ 标题防止孤行：`text-wrap: balance` 或 `text-pretty`

---

### **内容处理 (Content Handling)**

- ✅ 文本容器要处理长内容：`truncate`, `line-clamp-*`, 或 `break-words`
- ✅ Flex 子容器需要 `min-w-0` 才能让文字截断生效
- ✅ 处理空白状态——别渲染空字符串/数组的破碎 UI
- ✅ 预期用户输入会很长——设计要容纳短、中、长三种情况

---

### **图片 (Images)**

- ✅ `<img>` 要显式 `width` 和 `height`（防止 CLS - Cumulative Layout Shift）
- ✅ 非关键图片：`loading="lazy"`
- ✅ 首屏关键图片：`priority` 或 `fetchpriority="high"`

---

### **性能 (Performance)**

- ✅ 大列表 (>50 项)：虚拟化（`content-visibility: auto`）
- ✅ 别在 render 中读 DOM（`getBoundingClientRect`, `offsetHeight` 等）
- ✅ 批量读写 DOM，别交错进行
- ✅ 优先用非受控输入；受控输入必须每次击键都很轻
- ✅ CDN/资源域名加 `<link rel="preconnect">`
- ✅ 关键字体：`<link rel="preload" as="font">` 加 `font-display: swap`

---

### **导航与状态 (Navigation & State)**

- ✅ URL 应反映状态——过滤、tab、分页、展开面板都在 query params（支持深链接）
- ✅ 链接用 `<a>` / `<Link>`（支持 Cmd/Ctrl+click, 中键点击）
- ✅ 破坏性操作需要确认模态框或撤销窗口——别直接执行

---

### **触摸与交互 (Touch & Interaction)**

- ✅ `touch-action: manipulation`（防止双击缩放延迟）
- ✅ `-webkit-tap-highlight-color` 有意设置
- ✅ 模态框/抽屉/Sheet：`overscroll-behavior: contain`
- ✅ 拖拽时：禁用文字选择，`inert` 标记被拖元素
- ✅ `autoFocus` 仅限桌面单一主输入，避免移动端使用

---

### **安全区与布局 (Safe Areas & Layout)**

- ✅ 全宽布局需要 `env(safe-area-inset-*)` 处理刘海屏
- ✅ 避免意外滚动条：`overflow-x-hidden` 在容器，修复内容溢出
- ✅ 优先用 Flex/Grid，别用 JS 测量尺寸

---

### **深色模式与主题 (Dark Mode & Theming)**

- ✅ `<html>` 加 `color-scheme: dark`（修正滚动条、输入框样式）
- ✅ `<meta name="theme-color">` 匹配页面背景
- ✅ Native `<select>`：显式设置 `background-color` 和 `color`（Windows 深色模式）

---

### **国际化 (Locale & i18n)**

- ✅ 日期/时间：用 `Intl.DateTimeFormat` 而不是硬编码格式
- ✅ 数字/货币：用 `Intl.NumberFormat` 而不是硬编码格式
- ✅ 检测语言：`Accept-Language` / `navigator.languages`，别用 IP

---

### **Hydration 安全 (Hydration Safety)**

- ✅ 有 `value` 的输入框要 `onChange`（或用 `defaultValue` 非受控）
- ✅ 日期/时间渲染：防止 hydration 失配（服务器 vs 客户端）
- ✅ `suppressHydrationWarning` 仅在真正必要时使用

---

### **悬停与交互状态 (Hover & Interactive States)**

- ✅ 按钮/链接要 `hover:` 状态（视觉反馈）
- ✅ 交互状态提高对比度：hover/active/focus 比普通状态更突出

---

### **文案与文本 (Content & Copy)**

- ✅ 主动语态：`"Install the CLI"` 而不是 `"The CLI will be installed"`
- ✅ 标题用 Title Case（Chicago 风格）
- ✅ 数字表达数量：`"8 deployments"` 而不是 `"eight"`
- ✅ 按钮文案具体（包含动词和对象）：`"Save API Key"` 而不是 `"Continue"`
- ✅ 错误文案包含修复步骤，不仅是问题描述
- ✅ 第二人称；避免第一人称
- ✅ 在空间受限时用 `&` 代替 `and`

---

## ❌ 反模式（必须避免）

- ❌ `user-scalable=no` 或 `maximum-scale=1`（禁用缩放）
- ❌ `onPaste` + `preventDefault`（禁用粘贴）
- ❌ `transition: all`（改为列出具体属性）
- ❌ `outline-none` / `outline: none` 不提供焦点替代品
- ❌ 内联 `onClick` 导航（用 `<a>` 而不是 `<div>` 或 `<span>`）
- ❌ 图片没有 `width` / `height`（导致 CLS）
- ❌ 大数组 `.map()` 不虚拟化
- ❌ 表单输入没 label
- ❌ 图标按钮没 `aria-label`
- ❌ 硬编码日期/数字格式（用 `Intl.*`）
- ❌ `autoFocus` 没有明确理由

---

## 🎵 VibeStem 设计特例

### 音频播放器 (WaveformPlayer / MultiTrackPlayer)

- ✅ 波形容器需要 `content-visibility: auto`（大文件防止卡）
- ✅ 播放/暂停按钮：`aria-label="播放"` / `aria-label="暂停"`
- ✅ 进度条（range input）：有关联的输出显示时间
- ✅ 音量滑块要视觉反馈（数字显示百分比）
- ✅ Keyboard 快捷键（Space 播放/暂停，← → 快进/快退）

### BPM 检测与 EQ 控制

- ✅ 数字输入（BPM、频率）用 `type="number"` 和 `inputmode="numeric"`
- ✅ EQ 频段标签清晰（32Hz, 64Hz, ... 16kHz）
- ✅ 拖拽调整频率响应时要实时预览，不要等松开鼠标
- ✅ 预设按钮：`aria-pressed` 标记当前状态

### 任务进度 (Generation / Separation)

- ✅ Spinner 或 Progress bar 要清晰的 `aria-live="polite"` 告知进度
- ✅ "Processing…" 这样的文案以 `…` 结尾
- ✅ 完成时立即显示结果，不要延迟

---

**基于:** Vercel Web Interface Guidelines v1.0.0 | 100+ 规则重点汇总
